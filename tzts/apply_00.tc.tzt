code { APPLY } ;
input { Stack_elt (map nat nat) {} ; Stack_elt (lambda (pair (map nat nat) int) nat) { DROP ; PUSH nat 1 } } ;
output { Stack_elt (lambda int nat) { PUSH (map nat nat) {} ; PAIR ; { DROP ; PUSH nat 1 } } }
